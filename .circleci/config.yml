version: 2.1

commands:
  setup:
    - run:
      name: Install
      command: yarn
jobs:
  build:
    executor: node
    
  steps:
    - setup
    
    - run:
      name: Lint
      command: yarn lint
     
    - run: yarn lint:lit
    
    - run:
      name: Test
      command: yarn test
